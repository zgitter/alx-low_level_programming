#include "main.h"
/**
 * add - takes two integers and adds them
 * @a: first number to add op
 * @b: second number to add op
 * Return: returns add result
 */
int add(int a, int b)
{
	return (a + b);
}
