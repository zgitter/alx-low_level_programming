#include<stdio.h>

/**
 * main - Description. main prints a string
 * Return: returns 0 when runs right
 */
int main(void)
{       printf("with proper grammar%c but the outcome is a piece of art%c\n", 44, 44);
	return (0);
}
