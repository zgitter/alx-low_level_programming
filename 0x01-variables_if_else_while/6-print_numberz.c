#include<stdio.h>
/**
 * main - This main generates a - z
 * Return: returns 0 if main  runs right otherwise 1
 */
int main(void)
{
	int i;

	for (i = 48; i <= 57; i++)
	{
		putchar(i);
	}
	i = 10;
	putchar(i);
	return (0);
}
