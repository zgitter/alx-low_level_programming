#include<stdio.h>
/**
 * main - This main generates a - z
 * Return: returns 0 if main  runs right otherwise 1
 */
int main(void)
{
	int n;
	char c;

	for (n = 97; n < 123; n++)
	{
		c = 'n'
		putchar(c);
	}
	putchar('10');
	return (0);
}
