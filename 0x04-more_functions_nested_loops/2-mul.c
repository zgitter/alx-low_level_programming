#include "main.h"
/**
 * mul - takes two intergers and returns their product
 * @a: first integer 
 * @b: second integer
 * Return: return the axb result
 */
int mul(int a, int b)
{
	return (a * b);
}
